# Entornos / secretos — nunca en el repo (cada subproyecto también tiene el suyo)
.env
.env.*
!.env.example

# Dependencias y builds
node_modules/
dist/

# Base de datos local y backups — datos reales, nunca en el repo
backend/db/*.db
backend/db/*.db-journal
backend/db/*.db-wal
backend/db/*.db-shm
backend/db/backups/

# Artefactos de sesión de desarrollo / capturas
claude-photos/
md/
# Notas de trabajo por sesión (planes/handoffs locales): no se versionan
docs/sesion*.md
