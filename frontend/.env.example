# URL base del backend.
#  - Local (dev): http://localhost:3001/api
#  - Producción (dominio único nokfi.app, Nginx proxyea /api → backend :3001): /api
# En producción frontend y backend comparten origen → /api (same-origin, sin CORS).
VITE_API_URL=http://localhost:3001/api
